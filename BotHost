local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("EqiiryHub | BotHost", "Sentinel")


local Tab = Window:NewTab("Bots")
local Section = Tab:NewSection("EqiiryHub | Bots")
Section:NewToggle("EqiiryHub1", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub2", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub3", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub4", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub5", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub6", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub7", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub8", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub9", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub10", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub11", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub12", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub13", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub14", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub15", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub16", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub17", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub18", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub19", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub20", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub21", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub22", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub23", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub24", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub25", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub26", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub27", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub28", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub29", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)
Section:NewToggle("EqiiryHub30", "Toggle to connect the bot to the server", function(state)
    if state then
        print("Toggle On")
    else
        print("Toggle Off")
    end
end)

local Tab = Window:NewTab("Actions")
local Section = Tab:NewSection("EqiiryHub | Actions")

local Tab = Window:NewTab("Import")
local Section = Tab:NewSection("EqiiryHub | Target")

local Tab = Window:NewTab("Extra")
Section:NewLabel("Created By: Eqiiry#1337")
local Section = Tab:NewSection("EqiiryHub | Extras")
Section:NewKeybind("KeybindText", "KeybindInfo", Enum.KeyCode.F, function()
	Library:ToggleUI()
end)
Section:NewColorPicker("Color Text", "Color Info", Color3.fromRGB(0,0,0), function(color)
    print(color)
    -- Second argument is the default color
end)
Section:NewDropdown("Version", "DropdownInf", {"1.0.0", "1.0.1", "1.0.2"}, function(currentOption)
    print(currentOption)
end)
